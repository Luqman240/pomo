export type Session = {
    goal : string,
    workDuration : string,
    pauseDuration : string,
    loop : number,
    remainingLoop : number,
    remainingWorkDuration : string,
    remainingPauseDuration : string
}